# 项目协作规则

## 默认访问范围

以项目根目录的 `.ignore` 为唯一文件排除规则来源，不在其他规范中重复维护文件排除清单。

默认不搜索、不读取 `.ignore` 规则排除的文件或目录，包括项目浏览、全文检索和上下文收集。无论工具是否支持 `.ignore`，都应遵守此约定，不要通过关闭忽略规则、直接指定路径或更换工具来绕过。

用户明确要求访问被排除的内容时，仅访问该请求所需的文件或目录。若完成任务必须读取其中的内容，但用户尚未明确要求访问，应先说明原因并征得用户同意。

`.ignore` 未排除的内容，仍须满足适用的访问授权规则。对 `docs/` 的目录授权限制，按下述导航读取对应规范。

正常遵守本节规则时静默执行，仅在需要用户授权访问被排除内容时说明。

## docs 目录按需读取

在搜索、浏览、列举、读取、新建、修改或迁移 `docs/` 内容前，先完整读取 [docs目录读写授权规范](docs/specs/docs目录读写授权规范.md)。同一会话已读且未变化时，无须重复读取。

AI 执行的功能任务准备提交完成交付结果、收到明确取消决定，或负责人确认功能阶段结束时，即使本次未访问 `docs/`，也须读取上述规范并执行第 6 章的文档收敛检查。同一会话已读且未变化时，无须重复读取。

除上述结束节点外，仅提及文档路径、且不实际访问或操作 `docs/` 时，不触发读取。读取本规范不产生额外授权；文件排除和产物类型授权仍分别遵守本文件的对应规则。

## 写入与执行授权

文件可访问性不代表已经获得写入或执行授权。文件写入还应按产物类型和实际影响分别判断，不得在不同类型之间推导授权。

1. 用户要求讨论、分析、解释、评审、诊断、调研或提供方案时，默认只在对话中回答，不写入任何文件。
2. 用户明确要求记录、整理、撰写或更新文档时，仅授权修改完成该文档任务所必需的文档文件，不授权修改代码、测试、依赖、工程配置、数据库迁移、生成产物或数据。
3. 用户明确要求实现、修复或修改代码时，才授权修改完成目标所必需的代码及相关测试。该授权不自动包含依赖升级、构建配置、CI/CD、部署配置、数据库迁移或数据修改；任务未明确涵盖这些内容且确有必要时，应先说明原因、影响范围和风险，并征得用户同意。
4. 方案文档中的实施步骤、TODO、建议项、验收标准和示例代码均不构成执行授权。“写出实施方案”不等于“实施该方案”，“在文档中提供代码示例”也不等于“将示例写入项目代码”。
5. 不能仅按文件扩展名判断产物类型。依赖清单、构建与部署配置、CI/CD 配置、数据库迁移、脚本、提示词或 Agent 指令、API Schema 和代码生成配置等，即使以文本形式存在，也应按其实际运行影响处理。
6. 即使获得某一类型的写入授权，也仅允许进行完成当前目标所需的最小修改，不得顺带重构、清理、升级依赖或修改其他类型的产物。若需扩大修改范围，应先征得用户同意。
7. 同一请求明确包含文档与实现要求时，可以分别执行；完成后应分别说明文档改动、代码或工程改动，以及各自的验证状态。未运行必要测试或无法证明验证通过时，不得宣称任务已完成验证。

## 终态文档写作

编写或更新执行方案等终态文档时，直接记录结论和执行所需信息，不写入讨论过程、决策推演过程或其他过程性的描述性解释。确需保留此类内容时，应先提醒用户确认其写入方式：纳入终态文档的专题章节，或另行建立独立的决策文档。

## WI 工作项按需读取

当任务涉及创建、拆分、执行或恢复 WI，维护其字段、状态或记录，检查其结构、准入、委托或验收，或调整 WI 规范及模板时，在作出相关判断或行动前，先读取 [WI工作项维护规范](docs/specs/WI工作项维护规范.md) 第 6 章，再按当前场景读取必要章节和资料；不得默认加载整份规范。

仅提及 WI、列出 WI 文件，或摘取指定 WI 的标题、字段及原文时，不自动加载维护规范；需要解释状态含义、判断合规性或提出维护建议时，再触发上述导航。普通任务不因属于项目工作就自动进入 WI 流程。

关联资料仅在当前判断或动作需要时读取，不递归展开链接。读取导航不产生访问、修改、执行或 WI 维护授权，仍须遵守默认访问范围和写入与执行授权规则。

## 编码时按需读取

仅在即将编写或修改代码、测试、依赖或工程配置时，先读取 `docs/specs/技术规范.md`；纯讨论、调研、方案及文档编辑不自动读取。读取该规范仍受“默认访问范围”约束。
